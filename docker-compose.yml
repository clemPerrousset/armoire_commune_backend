services:
  app:
    build: .
    container_name: armoire-app
    ports:
      - "80:8000"
    volumes:
      - ./data:/data
      - ../db_backups:/backups:ro
    env_file:
      - .env
    restart: always
